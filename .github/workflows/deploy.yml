name: Deploy Bot

on:
  push:
    branches: [ clean-main ]

jobs:
  deploy:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v2
      
    - name: Setup Python
      uses: actions/setup-python@v2
      with:
        python-version: '3.13'
        
    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt
        
    - name: Test bot
      run: |
        echo "✅ Бот готов к деплою!"
        echo "🚀 Система подарочных генераций активна"
        echo "🎁 Новые пользователи получают 3 видео + 3 фото бесплатно"
